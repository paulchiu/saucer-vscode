{
  "name": "saucer",
  "displayName": "Saucer",
  "description": "VSCode extension that copies code reference with Git provider source links as Markdown for sharing.",
  "repository": "https://github.com/paulchiu/saucer-vscode",
  "version": "1.0.0",
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.99.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:typescript",
    "onLanguage:typescriptreact"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Saucer",
      "properties": {
        "saucer.includeRelativePath": {
          "type": "boolean",
          "default": true,
          "description": "References should include relative file path"
        },
        "saucer.linkSource": {
          "type": "boolean",
          "default": true,
          "description": "References should link source"
        },
        "saucer.cursorReferenceType": {
          "type": "string",
          "enum": [
            "Ask",
            "Symbol",
            "Filename"
          ],
          "default": "Ask",
          "description": "When referencing cursor position, use"
        },
        "saucer.selectionReferenceType": {
          "type": "string",
          "enum": [
            "Ask",
            "Symbol",
            "Filename"
          ],
          "default": "Ask",
          "description": "When referencing a selection, use"
        }
      }
    },
    "commands": [
      {
        "command": "saucer.copy",
        "title": "Copy Reference"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "saucer.copy",
          "group": "clipboard",
          "when": "editorTextFocus"
        }
      ]
    },
    "keybindings": [
      {
        "command": "saucer.copy",
        "key": "alt+shift+c",
        "mac": "alt+shift+c"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src && knip",
    "lint:fix": "eslint src --fix && knip --fix",
    "fmt": "prettier -w .",
    "test:vscode": "vscode-test",
    "test:unit": "vitest run unit.test.ts",
    "test:unit:watch": "vitest watch run unit.test.ts"
  },
  "devDependencies": {
    "@types/node": "20.x",
    "@types/vscode": "^1.99.0",
    "@typescript-eslint/eslint-plugin": "^8.28.0",
    "@typescript-eslint/parser": "^8.28.0",
    "@vscode/test-cli": "^0.0.10",
    "eslint": "^9.23.0",
    "knip": "^5.50.5",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "ts-pattern": "^5.7.0"
  }
}